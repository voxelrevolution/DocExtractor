# DocExtractor example environment
#
# NOTE: The FastAPI app does not auto-load this file unless you source it.
# Use: `set -a && source .env && set +a`

# API
PORT=8000

# Persistence (SQLite)
# Set to 1 to persist documents/extractions across restarts.
DOCEXTRACTOR_DB_ENABLED=0
# Optional override (defaults under ./data/db/)
DOCEXTRACTOR_DB_PATH=

# Ollama (local-only)
DOCEXTRACTOR_OLLAMA_ENABLED=1
DOCEXTRACTOR_OLLAMA_HOST=http://localhost:11434
DOCEXTRACTOR_OLLAMA_MODEL=llama3.1:8b
DOCEXTRACTOR_OLLAMA_TIMEOUT_S=20

# Batch ingest script defaults
DOCEXTRACTOR_API_BASE=http://127.0.0.1:8000
DOCEXTRACTOR_INGEST_ENDPOINT=/api/documents/ingest
DOCEXTRACTOR_INGEST_TIMEOUT_S=60
DOCEXTRACTOR_INGEST_PROGRESS=

# T04.4.4_JD-AGENT007_SafetyGuardrails

**JD used:** AGENT-007 (Agent Safety and Policy Lead)

**Requirement:** R04.4.1 – Copilot Chat

## Objective
Enforce local-only inference guardrails and safe fallbacks for `/api/chat`.

## Acceptance Criteria
- Ollama host is restricted to local hostnames (`localhost`, `127.0.0.1`, `::1`).
- All network calls use explicit timeouts.
- If inference fails, the system falls back safely (no crashes; user-friendly error path in UI).
- No secrets are required or logged.

## Notes
- This task focuses on safety posture and policy-as-code style checks within the application code.

# Evidence: T04.4.4_JD-AGENT007_SafetyGuardrails

## Guardrails Implemented
- Local-only host allowlist enforced for Ollama (`localhost`, `127.0.0.1`, `::1`).
- Explicit timeouts for HTTP calls.
- Graceful fallback when Ollama is unavailable or errors.

## Where Implemented
- Local-only URL validation: [src/llm/ollama_http.py](Reserved/DocExtractor/src/llm/ollama_http.py)
- Fallback behavior: [src/main.py](Reserved/DocExtractor/src/main.py)

## Notes
- No secrets are required; the integration uses the local Ollama server.

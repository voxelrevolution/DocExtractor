# DocExtractor UI example environment (Vite)
VITE_API_BASE_URL=http://localhost:8000

# Optional: scope corpus/extractions to a single client
# (passed through to the backend as client_id)
VITE_DOCEXTRACTOR_CLIENT_ID=
